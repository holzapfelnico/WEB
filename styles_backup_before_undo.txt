html, body { height: 100%; margin: 0; }
body {
  font-family: "Helvetica Neue Medium Extended", "HelveticaNeueLT Std Ext", "HelveticaNeueLT Pro Ext",
               "HelveticaNeue MediumExt", "Helvetica Neue", Helvetica, Arial, sans-serif;
  font-weight: 500;               /* Medium */
  font-stretch: expanded;         /* Extended (if supported by the font) */
  background: #000;
  transition: background-color .25s ease;
}

.board {
  position: relative;
  height: 100vh;
  overflow: hidden;
  background: #000;
  transition: background-color .25s ease, background .25s ease;
  touch-action: none; /* allow smooth drag/resize on touch devices */
}

/* Ensure foreground content sits above background */
.board { z-index: 1; }

/* (custom cursor removed) */

/* Placeholder text shown in project mode */
#project-placeholder {
  position: absolute;
  /* occupy the right half, with a big margin around */
  top: 8vmin;
  right: 8vmin;
  bottom: 8vmin;
  left: 55%;
  display: none; /* only visible in project mode */
  font-size: calc((100vh / 55) * 1.2);   /* upscale by 20% */
  line-height: 1.5;
  font-weight: 500;              /* Medium */
  font-stretch: expanded;        /* Extended */
  white-space: pre-line;         /* treat line breaks as paragraphs */
  pointer-events: none; /* do not block dragging photos */
  z-index: 999999; /* always above photos/videos */
}

.photo {
  --x: 50%;
  --y: 50%;
  --rot: 0deg;
  --w: 18vmin;
  position: absolute;
  left: var(--x);
  top: var(--y);
  transform: translate(-50%,-50%);
  display: block;
  width: var(--w);
  text-decoration: none;
  border-radius: 6px;
  transition: transform .12s ease;
  cursor: grab;
  user-select: none;
  will-change: transform; /* hint GPU for smoother moves */
}

.photo:hover { transform: translate(-50%,-50%) scale(1.02); }
.photo.dragging { cursor: grabbing; }
/* Show resize cursor when hovering near photo border */
.photo.edge-hover,
.photo.resizing { cursor: nwse-resize; }

/* Prefix label (e.g., 0007) at top-right */
.prefix-tag {
  position: absolute;
  top: 6px;
  right: 6px;
  background: none;
  border: 0;
  padding: 0;
  margin: 0;
  color: #000;
  font-family: inherit;          /* match global font */
  font-size: 2vh;
  font-weight: 700; /* bold */
  text-decoration: none;
  line-height: 1;
  cursor: default;
  text-shadow: none;
  user-select: none;
  display: none; /* hidden until hover */
  pointer-events: none; /* allow clicks to pass to image */
}
.photo:hover .prefix-tag { display: block; }

/* (mode-based text colors removed) */

/* Top navigation bar */
.topbar {
  position: sticky;
  top: 0;
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 12px;
  background: transparent;
  font-weight: 700;
  font-size: calc(100vmin / 12);
  line-height: 1;
}
.topbar .brand { user-select: none; cursor: pointer; }
.topbar .nav { display: flex; gap: 16px; }
.topbar .nav button {
  appearance: none;
  background: none;
  border: 0;
  padding: 0;
  margin: 0;
  font: inherit;
  color: inherit;
  cursor: pointer;
}

.photo img {
  display: block;
  width: 100%;
  height: auto;
  border-radius: 6px;
  pointer-events: none;
  user-select: none;
  box-sizing: border-box;
}

/* Keep videos styled like images */
.photo video {
  display: block;
  width: 100%;
  height: auto;
  border-radius: 0; /* remove curved border on videos */
  pointer-events: none;
  user-select: none;
  box-sizing: border-box;
}

.hint {
  position: absolute;
  right: 16px;
  bottom: 16px;
  background: rgba(0, 0, 0, 0.08);
  border: 1px solid rgba(0,0,0,.08);
  border-radius: 6px;
  font-size: 13px;
  padding: 6px 8px;
  user-select: none;
  pointer-events: none;
}

@media (max-width: 640px) {
  .hint { display: none; }
  #project-placeholder {
    /* Increase current mobile size by additional 20% */
    font-size: calc(((100vh / 110) * 1.3) * 1.2);
    /* Make text area slightly wider on mobile */
    left: 50%;
  }
  /* Remove curved frames on mobile */
  .photo, .photo img, .photo video { border-radius: 0; }
  /* Background video removed on mobile as well */
}

/* Back button centered at lower border */
#back-button {
  position: fixed;
  left: 50%;
  bottom: calc(env(safe-area-inset-bottom, 0px) + 4vmin);
  transform: translateX(-50%);
  z-index: 2100;
  appearance: none;
  background: none;
  border: 0;
  padding: 0;
  margin: 0;
  color: inherit;
  font-family: inherit;
  font-weight: 500;              /* similar to placeholder */
  font-stretch: expanded;        /* if available */
  font-size: calc(100vh / 55);   /* similar to placeholder text */
  line-height: 1.2;
  cursor: pointer;
}

/* Improve mobile usability */
@media (hover: none), (pointer: coarse) {
  body { -webkit-tap-highlight-color: transparent; }
  /* Make the prefix tag visible on touch devices (still non-interactive) */
  .photo .prefix-tag { display: block; }
}

/* Info Overlay */
#info-overlay {
  position: fixed;
  inset: 0;
  z-index: 3000;                /* above topbar/photos */
  display: block;
  background: rgba(0,0,0,0.25); /* transparent dark veil */
  backdrop-filter: blur(8px);   /* blur everything behind */
  opacity: 0;
  pointer-events: none;         /* non-interactive when hidden */
  transition: opacity .25s ease;
}

#info-overlay[data-open="true"] {
  opacity: 1;
  pointer-events: auto;         /* interactive when shown */
}

.info-content {
  position: absolute;
  top: 8vmin;
  right: 8vmin;
  bottom: 8vmin;
  left: 55%;
  font-size: calc((100vh / 55) * 1.2);
  line-height: 1.5;
  font-weight: 500;
  font-stretch: expanded;
  white-space: pre-line;
  color: inherit;              /* follow current site text color */
  pointer-events: auto;
}

@media (max-width: 640px) {
  .info-content {
    font-size: calc(((100vh / 110) * 1.3) * 1.2);
    left: 50%;
  }
}

.project-menu {
  display: flex;
  flex-direction: column;
  gap: 1.2em;
}

.project-menu-title {
  font-size: 0.9em;
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

.project-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 0.6em;
  font-size: 0.8em;
  line-height: 1.35;
}

.project-item {
  margin: 0;
}

.project-button {
  appearance: none;
  border: 1px solid currentColor;
  background: transparent;
  color: inherit;
  padding: 0.35em 0.75em;
  font: inherit;
  font-size: 1em;
  text-align: left;
  width: 100%;
  cursor: pointer;
  border-radius: 999px;
  display: inline-flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.75em;
  transition: background-color .2s ease, color .2s ease;
}

.project-button:hover,
.project-button:focus {
  background: rgba(255,255,255,0.12);
  outline: none;
}

.project-button:focus-visible {
  outline: 2px solid currentColor;
  outline-offset: 3px;
}

.info-columns {
  display: flex;
  align-items: flex-start;
  gap: 3em;
}

.info-columns .cv-intro {
  flex: 0 0 22ch;
  max-width: 22ch;
}

.info-columns .project-menu {
  flex: 1;
}

@media (max-width: 640px) {
  .info-columns {
    flex-direction: column;
    gap: 1.5em;
  }

  .info-columns .cv-intro {
    flex: none;
    max-width: none;
  }
}

